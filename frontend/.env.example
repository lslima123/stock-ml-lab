# Leave empty when the frontend and FastAPI share an origin or when using the
# Vite dev proxy. For a separately deployed API, use e.g.:
# VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
